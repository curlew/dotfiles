- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.zshenv: zshenv
    ~/.dircolors: dircolors

- create:
    - ~/src

- shell:
    - git submodule update --init --recursive
